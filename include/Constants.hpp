#ifndef CONSTANTS_HPP
#define CONSTANTS_HPP

const int MAX_DIFFICULTY = 15;
const int TARGET_TIME_PER_BLOCK = 1; // in seconds
const int ADJUST_DIFFICULTY_INTERVAL = 5; // adjust difficulty each 5 blocks

#endif
