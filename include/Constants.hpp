#ifndef CONSTANTS_HPP
#define CONSTANTS_HPP

inline constexpr int MAX_DIFFICULTY = 5;
inline constexpr int TARGET_TIME_PER_BLOCK = 1; // in seconds
inline constexpr int ADJUST_DIFFICULTY_INTERVAL = 5; // adjust difficulty each 5 blocks

#endif
